导出远程数据库：
npx wrangler d1 export a --remote --output=./db.sql

导入本地数据库：
npx wrangler d1 execute a --local --file=./db.sql

导出本地数据库：
npx wrangler d1 export a --local --output=./db.sql

导入远程数据库：
npx wrangler d1 execute a --remote --file=./db.sql

wrangler.jsonc
{
    "name": "a",
    "main": "src/app.tsx",
    "compatibility_date": "2025-07-01",
    "assets": {
        "directory": "./public",
        "binding": "ASSETS"
    },
    "d1_databases": [
        {
            "binding": "DB",
            "database_name": "a",
            "database_id": "?"
        }
    ],
    "vars": {
        "ImgBBKey": "?"
    }
}
